[Unit]
Description=Root0 - Set TCP TIME_WAIT buckets
DefaultDependencies=no
Before=network.target
After=local-fs.target

[Service]
Type=oneshot
ExecStart=/usr/sbin/sysctl -w net.ipv4.tcp_max_tw_buckets=1440000
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target 